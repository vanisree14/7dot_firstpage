**Input** — single-line text field with optional leading icon and teal focus ring. Pair with `Field` for labels/hints.

```jsx
<Field label="Email" hint="We'll send your itinerary here">
  <Input type="email" placeholder="you@example.com" />
</Field>
```
Input props: `icon`, `invalid`, plus all native input attrs.
