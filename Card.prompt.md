**Card** — base white surface container. Wrap content blocks; set `interactive` for clickable cards that lift on hover.

```jsx
<Card interactive>
  <h3 className="rv-h3">Wonder First</h3>
  <p>Every journey begins with awe.</p>
</Card>
```
Props: `interactive`, `padding`, plus div attrs.
