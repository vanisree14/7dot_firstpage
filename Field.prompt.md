**Field** — labelled wrapper around any control (Input, Select, etc.) with optional hint or error text.

```jsx
<Field label="Departure city" required error="Required">
  <Input placeholder="Where do you start?" />
</Field>
```
Props: `label`, `hint`, `error`, `required`.
