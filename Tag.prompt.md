**Tag** — filter/category chip. Use in toolbars and filter rows; pass `onClick` to make it toggle.

```jsx
<Tag selected onClick={...}>Mountain</Tag>
<Tag icon={<span>★</span>} onClick={...}>Surprise routes</Tag>
```
Props: `selected`, `icon`, `onClick`.
