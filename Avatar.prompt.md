**Avatar** — round user/trip avatar with image or initials fallback. Add `ring` for featured guides / "wow" hosts.

```jsx
<Avatar name="Maya Rao" size={48} />
<Avatar src="/guide.jpg" name="Arjun" ring />
```
Props: `src`, `name`, `size` (px), `ring`.
